def parse_pointset(data: bytes):
    """Parse un flux binaire en liste de points (x, y)."""
    raise NotImplementedError("parse_pointset pas encore implémentée")

def serialize_pointset(points):
    """Sérialise une liste de points vers un flux binaire."""
    raise NotImplementedError("serialize_pointset pas encore implémentée")

def parse_triangles(data: bytes):
    """Parse un flux binaire en vertices + triangles."""    
    raise NotImplementedError("parse_triangles pas encore implémentée")